type Quiz {
  id: ID! @id
  title: String!
  description: String
  createdAt: DateTime! @createdAt
  choices: [Choice!]! @relation(link: INLINE)
}

type Choice {
  id: ID! @id
  createdAt: DateTime! @createdAt
  quiz: Quiz!
  title: String!
}
